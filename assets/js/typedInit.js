document.addEventListener("DOMContentLoaded", function() {
  var typed = new Typed('.rotating-word', {
    strings: ["convert", "engage", "evolve", "work"],
    typeSpeed: 50,
    backSpeed: 5,
    backDelay: 5000,
    loop: false,
    smartBackspace: false,
    showCursor: true,
    cursorChar: '.'
  });
});
